---
/** Two-layer ornamental initial: an outlined italic ghost behind a solid roman letter. */
interface Props {
  letter?: string;
  class?: string;
}
const { letter = 'A', class: className } = Astro.props;
---

<span class:list={['monogram', className]} data-letter={letter} aria-hidden="true">{letter}</span>
